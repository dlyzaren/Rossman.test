package sayfalar;

import org.openqa.selenium.WebElement;

public class not  {} //for (int i = 0; i <= 3; i++) {
// i değişkeni 1, 2, 3 ve 4 olacak şekilde dönecek
/// liElement = teslimatBilgileri.get(i);
//String classValue = liElement.getAttribute("class");
          // if (classValue != null && !classValue.isEmpty()) {
        //classList.add(liElement.getText());
       // }
       // }

        // Toplanan class değerlerini ekrana yazdırın
       ///for (String className : classList) {
     ///  System.out.println(className);
     //  / }{
//}
